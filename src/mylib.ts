
/**
 * hello_ returns a string that concatenates the given name
 * to the prefix 'Hello '.
 *
 * @function
 * @param name The name to be prefixed by "Hello "
 *
 */
function hello_(name: string): string {
  return `Hello ${name}`;
}

export { hello_ };
